class Identifiers {
     public int numbars() {
         int x = 3;
         int y;
         //int z = x + y; //before use, MUST be initiazlized!
         System.out.println(x);
         return x;
     }
}
